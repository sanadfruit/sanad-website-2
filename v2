<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Hassan Bin Ali Al Sanad & Partners Co.</title>
  <!-- Google Fonts & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Cairo:wght@300;400;600&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <style>
    /* Reset & Variables */
    * { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --primary: #66bb6a;
      --accent: #a5d6a7;
      --bg: #fcfdfc;
      --text: #333;
      --light: #fff;
      --font-main: 'Poppins', sans-serif;
      --font-ar: 'Cairo', sans-serif;
      --trans: 0.6s ease;
    }
    body { font-family: var(--font-main); color: var(--text); background: var(--bg); line-height: 1.6; overflow-x: hidden; }
    a { color: var(--primary); transition: color var(--trans); }
    a:hover { color: var(--accent); }
    img { max-width: 100%; display: block; }
    /* Header */
    header { position: sticky; top: 0; background: var(--light); backdrop-filter: blur(10px); z-index: 1000; }
    .container { max-width: 1200px; margin: auto; padding: 0 1rem; }
    nav { display: flex; justify-content: space-between; align-items: center; height: 60px; }
    .logo { font-size: 1.5rem; font-weight: 600; color: var(--primary); }
    .nav-links { display: flex; gap: 1.5rem; }
    .nav-links a { position: relative; padding: .5rem; font-weight: 500; }
    .nav-links a::after { content: ''; position: absolute; left: 0; bottom: 0; width: 0; height: 2px; background: var(--primary); transition: width var(--trans); }
    .nav-links a:hover::after { width: 100%; }
    .menu-toggle { display: none; font-size: 2rem; cursor: pointer; color: var(--primary); }
    /* Hero Section with Animated Gradient Text */
    .hero { position: relative; height: 85vh; display: flex; align-items: center; justify-content: center; text-align: center; }
    .hero::before {
      content: '';
      position: absolute; top: 0; left: 0; width: 100%; height: 100%;
      background: url('images/hero.jpg') center/cover no-repeat;
      filter: brightness(0.6);
      z-index: -2;
    }
    .hero::after {
      content: '';
      position: absolute; bottom: -50px; left: 0; width: 100%; height: 100px;
      background: var(--bg);
      clip-path: ellipse(70% 100% at 50% 0%);
      z-index: -1;
    }
    .hero-content h1 {
      font-size: 3rem; font-weight: 700; background: linear-gradient(45deg, #8bc34a, #4caf50, #aed581);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
      animation: textShift 5s infinite linear;
    }
    .hero-content p { margin: 1rem 0; font-size: 1.2rem; }
    .btn { display: inline-block; background: var(--primary); color: var(--light); padding: .75rem 1.5rem; border-radius: 30px; font-weight: 600; transition: transform var(--trans), background var(--trans); }
    .btn:hover { transform: scale(1.05); background: var(--accent); }
    /* Section Headings with Organic Shapes */
    section { padding: 4rem 0; position: relative; }
    section:nth-of-type(even)::before {
      content: '';
      position: absolute; top: -50px; right: -100px; width: 200px; height: 200px;
      background: var(--accent); border-radius: 50% 50% 0 50%; opacity: .2;
    }
    h2 { font-size: 2.5rem; text-align: center; margin-bottom: 1rem; }
    /* Grid Layouts */
    .grid { display: grid; gap: 2rem; }
    .products .grid { grid-template-columns: repeat(auto-fit, minmax(250px,1fr)); }
    .services .grid, .gallery .grid { grid-template-columns: repeat(auto-fit, minmax(240px,1fr)); }
    /* Cards with 3D Tilt Effect */
    .card { background: var(--light); border-radius: 16px; overflow: hidden; perspective: 1000px; }
    .card-inner { transform-style: preserve-3d; transition: transform .4s; }
    .card:hover .card-inner { transform: rotateY(10deg) rotateX(5deg); }
    .card img { display: block; backface-visibility: hidden; }
    .card-content { padding: 1rem; }
    /* Gallery Masonry */
    .gallery .grid { grid-auto-flow: dense; grid-template-columns: repeat(auto-fit, minmax(180px,1fr)); }
    /* Testimonials Slider Placeholder */
    .testimonials { text-align: center; }
    /* Contact Form & Map */
    .contact { display: flex; flex-wrap: wrap; gap: 2rem; }
    .contact-form, .map { flex: 1 1 300px; }
    .contact-form label { display:block; margin-bottom:.5rem; }
    .contact-form input, .contact-form textarea { width:100%; padding:.75rem; margin-bottom:1rem; border:1px solid #ccc; border-radius:8px; }
    /* Arabic Section */
    .arabic { direction:rtl; text-align:right; font-family:var(--font-ar); background:var(--light); padding:2rem; border-radius:12px; }
    /* Footer */
    footer { background: var(--bg); text-align: center; padding: 2rem; font-size: .9rem; }
    /* Keyframe for gradient text */
    @keyframes textShift { 0%{background-position:0% 50%}50%{background-position:100% 50%}100%{background-position:0% 50%} }
    /* Responsive */
    @media(max-width:768px) { .menu-toggle { display:block; } .nav-links { display:none; flex-direction:column; gap:1rem; position:absolute; top:60px; left:0; right:0; background:var(--light); padding:1rem; } }
  </style>
</head>
<body>
  <header>
    <div class="container">
      <div class="logo">SANAD Partners</div>
      <div class="menu-toggle material-icons">menu</div>
      <ul class="nav-links">
        <li><a href="#about">About</a></li>
        <li><a href="#products">Products</a></li>
        <li><a href="#services">Services</a></li>
        <li><a href="#gallery">Gallery</a></li>
        <li><a href="#contact">Contact</a></li>
        <li><a href="#arabic">العربية</a></li>
      </ul>
    </div>
  </header>

  <section class="hero">
    <div class="hero-content">
      <h1>Delivering Freshness Since 1980</h1>
      <p>Your Trusted Partner in Dammam’s Wholesale Market</p>
      <a href="#contact" class="btn">Get a Quote</a>
    </div>
  </section>

  <section id="about" class="container">
    <h2>About Us</h2>
    <p>Since 1980, Hassan Bin Ali Al Sanad & Partners Co. has supplied premium fruits & vegetables from Dammam’s central market. Our new delivery service brings market-fresh produce directly to your door across the Eastern Province.</p>
  </section>

  <section id="products" class="products container">
    <h2>Our Products</h2>
    <div class="grid">
      <div class="card"><div class="card-inner"><img src="images/veg1.jpg" alt="Mixed Vegetables"></div><div class="card-content"><h3>Mixed Vegetables</h3><p>Tomatoes, Potatoes, Peppers &amp; more.</p></div></div>
      <div class="card"><div class="card-inner"><img src="images/greens2.jpg" alt="Fresh Greens"></div><div class="card-content"><h3>Fresh Greens</h3><p>Mint, Parsley, Coriander &amp; specialty greens.</p></div></div>
      <div class="card"><div class="card-inner"><img src="images/fruits3.jpg" alt="Seasonal Fruits"></div><div class="card-content"><h3>Seasonal Fruits</h3><p>Bananas, Apples, Watermelons &amp; exotic imports.</p></div></div>
    </div>
  </section>

  <section id="services" class="services container">
    <h2>Our Services</h2>
    <div class="grid">
      <div class="card"><div class="card-inner"><img src="images/service1.jpg" alt="Daily Sourcing"></div><div class="card-content"><h3>Daily Sourcing</h3><p>Fresh market procurement every morning.</p></div></div>
      <div class="card"><div class="card-inner"><img src="images/service2.jpg" alt="Quality Grading"></div><div class="card-content"><h3>Quality Grading</h3><p>Rigorous sorting to your standards.</p></div></div>
      <div class="card"><div class="card-inner"><img src="images/service3.jpg" alt="Custom Orders"></div><div class="card-content"><h3>Custom Orders</h3><p>Packed exactly how you need.</p></div></div>
      <div class="card"><div class="card-inner"><img src="images/service4.jpg`
